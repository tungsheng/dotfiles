#!/bin/sh

tmux list-panes -F "#{pane_index}" |
    while read -r pane; do
        echo $pane
        tmux select-pane -t $window.$pane
        tmux send-keys C-c
        tmux send-keys ',Q'
    done
tmux kill-window
